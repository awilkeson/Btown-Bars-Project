//
//  BrothersFavoriteSwitch.swift
//  capstone_btownbars
//
//  Created by Andrew Frisinger on 3/24/19.
//  Copyright © 2019 Ashley Wilkeson. All rights reserved.
//

import UIKit

class BrothersFavoriteSwitch: UISwitch {

    
    
    
    
    
    
    
    
    
    
    /*  @IBOutlet weak var BarFavorites: UISwitch!
     @IBOutlet weak var label: UILabel!
     
     override func viewDidLoad() {
     super.viewDidLoad()
     
     changeText()
     BarFavorites.addTarget(self, action: #selector(switchToggled(_:)), for: UIControl.Event.valueChanged)
     }
     
     @IBAction func switchToggled(_ sender: UISwitch) {
     changeText()
     }
     
     func changeText() {
     if BarFavorites.isOn {
     label.text = "Added"
     } else {
     label.text = "Not Yet Added"
     }
     }
     
     
     */
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    /*    var isOn = false
    override init(frame: CGRect) {
        super.init(frame: frame)
        initButton()
    }
    
    required init?(coder aDecoder: NSCoder) {
        super.init(coder: aDecoder)
        initButton()
    }
    
    func initButton() {
        layer.borderWidth = 2.0
        layer.borderColor = Colors.barOrange.cgColor
        layer.cornerRadius = frame.size.height/2
        
        setTitleColor(Colors.barOrange, for: .normal)
        addTarget(self, action: #selector(BrothersFavoriteSwitch.buttonPressed), for: .touchUpInside)
    }
    
    @objc func buttonPressed() {
        activateButton(bool: !isOn)
    }
    
    func activateButton(bool: Bool) {
        
        isOn = bool
        
        let color = bool ? Colors.barOrange : .clear
        let title = bool ? "+" : "+"
        let titleColor = bool ? . white : Colors.barOrange
        
        setTitle(title, for: .normal)
        setTitleColor(titleColor, for: .normal)
        backgroundColor = color
        
        
    }
 */
}
